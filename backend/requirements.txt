fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
aiofiles>=23.2

# FunASR（国内 ASR + 说话人分离，模型从 ModelScope 下载，推荐）
funasr>=1.0
modelscope

# PyTorch — FunASR 模型推理需要（start.bat/sh 会自动处理版本）
# torch / torchaudio 由启动脚本安装，此处不重复声明

# WhisperX（可选，仅当使用 WhisperX 引擎时需要）
# 注意：需手动 pip install whisperx torch torchaudio

# Audio
pydub>=0.25

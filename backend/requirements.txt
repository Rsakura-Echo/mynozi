fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
aiofiles>=23.2

# FunASR（仅用于句子切割/框选识别，模型从 ModelScope 下载）
funasr>=1.0
modelscope

# PyTorch — 模型推理需要（start.bat/sh 会自动处理版本）
# torch / torchaudio 由启动脚本安装，此处不重复声明

# WhisperX — ASR 主引擎，由设置页「下载模型」按需安装
# 安装逻辑见 routers/settings.py:_install_whisperx()

# Audio
pydub>=0.25
